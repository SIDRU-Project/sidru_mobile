<?xml version="1.0" encoding="utf-8"?>
<!--
  Política de tráfico de red de SIDRU.

  Por defecto se PROHÍBE el HTTP plano: la app habla con la API por HTTPS
  (Caddy + Let's Encrypt en la VM de Azure). Así las credenciales del ciudadano
  y el JWT nunca viajan en claro, y Google Play no marca cleartext traffic.

  La excepción son los hosts de desarrollo local, donde no hay certificado:
  10.0.2.2 es el host visto desde el emulador de Android. Si pruebas contra un
  backend en tu LAN desde un teléfono físico, añade aquí esa IP (p. ej.
  192.168.1.50) y quítala antes de generar el release.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
